# AI Capability Market

一个面向 AI 能力交易平台的 React 界面原型，适合上传 GitHub 后用 Vercel 直接部署。

## 本地运行

```bash
npm install
npm run dev
```

## Vercel 部署

把仓库导入 Vercel，保持默认设置即可：

- Framework Preset: `Vite`
- Build Command: `npm run build`
- Output Directory: `dist`
